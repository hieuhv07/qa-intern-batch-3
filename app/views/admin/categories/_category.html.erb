<tr>
	<td><%= count_index_list category_counter %></td>
	<td>
		<a href="<%= edit_admin_category_path(category) %>"><%= category.name %></a>
	</td>
	<td><%= category.description %></td>
	<td>
		<%= link_to "Delete", admin_category_path(category), class: "btn btn-danger", method: :delete, data: {confirm: t(".sure")} %>
	</td>
</tr>